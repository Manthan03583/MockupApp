<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MockupApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MockupApp" parent="Base.Theme.MockupApp" />

    <style name="AliciaProfileToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:actionMenuTextColor">@color/white</item>
        <item name="android:actionOverflowButtonStyle">@style/CustomOverflowButtonStyle</item>
    </style>

    <style name="CustomOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/alicia_profile_toolbar_menu_button</item>
    </style>
</resources>